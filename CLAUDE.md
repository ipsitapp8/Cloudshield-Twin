past these rules : 

Project: CloudShield Twin. SPEC.md is the source of truth; follow it exactly, don't invent features.
Python 3.11, type hints, pydantic v2, networkx. engine/ is pure (no I/O).
Write pytest tests for every engine function using fixtures/. Run tests before saying done.
Keep code small. No new dependency without saying why. Reply with the code changes + a 3-line summary only.
Never run destructive commands. Stop after the requested phase.